l do Administrador Master</h1> <h3>Usuários pendentes de aprovação</h3> {pendingUsers.length === 0 && <p>Nenhum usuário pendente.</p>} {pendingUsers.map(user => ( <div key={user.id} style={{ marginBottom: 20, padding: 15, border: '1px solid #ccc' }}> <strong>{user.name}</strong> — {user.email} <br /> <button onClick={() => approveUser(user.id, 'admin')} style={{ marginRight: 10 }}>Aprovar como Admin</button> <button onClick={() => approveUser(user.id, 'therapist')} style={{ marginRight: 10 }}>Aprovar como Terapeuta</button> <button onClick={() => approveUser(user.id, 'patient')} style={{ marginRight: 10 }}>Aprovar como Paciente</button> <button style={{ color: 'red' }} onClick={() => rejectUser(user.id)}> Rejeitar Usuário </button> </div> ))} </div> ); } 
"@
